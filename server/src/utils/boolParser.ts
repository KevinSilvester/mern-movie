import type { Request, Response, NextFunction } from 'express'

// const boolParser = () => (req: Request, res: Response, next: NextFunction) => {
//    const result = {}
//    let key, value

//    for (key in req.query) {
//       if (req.query.hasOwnProperty(key)) {
//          value = req.query[key]
//       }
//    }
// }